# Contributing

本项目仍处于实验阶段，许可证和公开仓库设置尚未确定。在这些事项确认前，不接收外部代码合入。问题讨论和文档建议也不要包含非公开资料。

## 报告问题

普通缺陷请提供：

- 使用的版本或 commit，以及 Python、操作系统和相关依赖版本；
- 可重现的最小步骤，使用项目合成案例或可重生成的虚构数据；
- 预期结果、实际结果和必要的错误片段；
- 已尝试的检查。

不要贴整个环境变量列表、数据库、自动化运行日志或公司截图。安全问题按 [SECURITY.md](./SECURITY.md) 处理。

## 本地验证

在仓库根目录建立环境：

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -e .
```

修改前先运行相关测试；修复应附带能在修复前失败的回归用例。提交验证记录时写明命令、结果和跳过项：

```bash
PYTHONPATH=src python3.11 -m unittest discover -s tests -v
PYTHONPATH=src python3.13 -m unittest discover -s tests -v
python3.11 -m compileall -q src tests
python3.13 -m compileall -q src tests
```

两条 Python 命令需要各自安装的解释器和依赖。不要将某个解释器下的结果写成两个版本均已通过。

涉及 OCR 或 Web 时，另运行本地演示、合成基准和对应的浏览器检查。Tesseract 是外部程序，不由 pip 安装。供应链检查的失败与测试失败分别记录，不为取得绿灯删除未知许可证等阻断项。

## 修改范围

一个 PR 处理一个问题，说明改动原因、回归用例和剩余限制。避免夹带无关格式化、依赖升级或生成文件。

以下约束不能因实现调整而改变：

- 首审全部完成并锁定后，才允许排队、运行或显示 AI；
- 原始字符串由确定性代码比较，模型输出不能当作真值；
- AI 不覆盖人工记录、不关闭异常、不自动放行；
- 定向异常复核与全字段盲二审分别实现和描述；
- 失败调用不能留下部分状态或追加事件。

新增依赖前，记录上游来源、许可证、版本和替代方案。参考别人的实现时注明来源；不要把参考项目的许可证自动套用到模型、数据或本项目。

## 文档写法

先写用途和可运行示例，再解释实现。每个能力描述应能对应到代码、测试或明确的限制。

- 用具体动词，如“比较”“拒绝”“保存”；少用“赋能”“全面保障”“企业级”等无法验证的形容。
- 区分“已实现”“已测试”“未接入”，不把计划写成成果。
- 安装命令从仓库根目录开始，不含个人电脑路径。
- 测试数字附环境和条件；跳过不算执行，拒答不算识别正确。
- 标题简短；一个段落只说明一件事。详细日志留在本地，不放进 README。
- 使用辅助工具生成的修改也需要提交者理解、复现和审查，不虚构个人贡献或测试结果。

参见[项目声明](./docs/CLAIMS_AND_LIMITATIONS.md)和[发布检查](./docs/PUBLISHING.md)。
